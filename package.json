{
  "name": "meiosis",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "gene-sim": "bun run scripts/gene-sim.ts",
    "test:contracts": "forge test --root contracts",
    "setup": "bun install && forge install foundry-rs/forge-std --no-git --root contracts"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "viem": "^2.21.0"
  }
}
